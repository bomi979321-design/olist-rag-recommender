# Python >= 3.14 required for local dev (pickle compatibility with pandas 3.x StringDtype)
# Streamlit Cloud uses Python 3.11+ which is compatible
streamlit>=1.35.0
chromadb>=1.5.0
sentence-transformers>=5.0.0
pandas>=2.0.0
numpy>=1.26.0
tqdm>=4.66.0
pyarrow>=16.0.0
